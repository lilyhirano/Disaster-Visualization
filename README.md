# Disaster-Visualization
Data200S Project

## TO START:

1. Create a folder to house the project (like `grad_proj`)
2. `git clone` #your ssh# `proj_code`
3. Add the data to the project folder (`grad_proj`).  Use the folder `sattelite-image-data`, which should include each disaster folder and the 2 npz files.
4.  Don't forget to create your own branch!